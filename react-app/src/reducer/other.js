

const chart = (state = [], action) => {
    let result = [...state]
    switch (action.type) {
        case "get":
            break;
        case "add":

            break;
        case "update":
            break;
        default:
            break;
    }
    return result;
}

const pageStatusManger = (type) => {
    switch (type) {
        case 1:
            break;
        default:
            return {
                a: ""
            }
    }
}

export default pageStatusManger